# Handoff → K6 dfaa 遷移波

**Created**: 2026-07-31。交班目標：新 session 開 K6（kit 品管通用化 7+1 波計畫第六波）。

我們正在做 **kit 品管通用化**——把 dfaa 的六層品質防線（41 gate script、44 ratchet counter、RTM 真理層、防假 0 族、merge train）抽成 `~/projects/skills/kit/` 通用引擎，專案只放宣告。總計畫：`~/.claude/plans/witty-hugging-sphinx.md`；詳細設計：`witty-hugging-sphinx-agent-ace953f2ac0b47679.md`（421 行，含 §6 K6 遷移四步）。

## 專案狀態

K1-K5 全部交付並 merge（skills repo main `c2cdf1c`）。全套 732 assertions 0 failed；`kit-audit` 同步 40 exit 0；**dfaa parity 已實證**（kit rtm-check 對 dfaa 真 matrix 315 條輸出逐位相同）。K6=dfaa 從自有腳本切到 kit 引擎（shim 過渡）；K7=第二梯隊+熱區生命週期；K8=autopilot。

## 已完成的大事

1. **K1** — manifest closed ledger（`kit/manifest/provenance.tsv` 8 欄雙 hash）+ `tools/kit-audit.sh` 三態判定 + `/kit-audit` skill
2. **K2** — 防假 0 族（canary harness 三斷言/pathspec-liveness/hook-wiring-check）+ merge-train 六支 + `engines/wave-gate.sh` 骨架（gates.d 協定）
3. **K34** — `engines/ratchet.sh`（counter 外移 counters.d，**D1-D9 差異表在引擎檔頭**）+ backlog 工具鏈五支（近 verbatim 含 encoding fix）
4. **K5** — RTM 模組（rtm-check.ts 參數化+--emit-index 讀側索引+reqsync 寫側+touch-reminder/session-start hooks）+ `installers/kit-init.sh` 8 模組 dispatcher + onboarding 訪談（kit SKILL.md 86 行）+ 安全審計 HIGH×2 已修
5. **wave skill 本體重構**（本 session 前段）— 780→639 行 progressive disclosure；dotfiles 鏡像**已過期**（K5 又動了 kit/wave/SKILL.md+templates.md，見待辦）

## 關鍵決策（本對話確認，未寫進計畫檔的細節）

- 引擎住 kit（絕對路徑消費）＋專案宣告 `.claude/kit/`；`base_branch` 是全 kit 統一 key（不是 tighten_branch/regate_branch）
- merge-train 驗證走 **selftest 路線**（使用者核准偏離雙 fixture）
- RTM 是核心真理層（「像真理一樣的存在」）＋雙側執法（寫側 reqsync 硬擋、讀側 touch-reminder fail-open 先觀察誤報率）
- manifest 無帳掃描範圍 = 受管七目錄 `*.sh ∪ *.ts`；templates/references 帳可選
- K7 加熱區生命週期（**反向白名單** permanent_files + git 時間戳判齡——檔名慣例不可靠）；K8 autopilot（tmux 開波+引擎判準+Telegram 選配降級）
- 每波流程紀律：Fable 諮詢點 1 必做（每次都抓到實質缺口）、controller 親跑 review 不採信報告、manifest 由 controller 統一記帳、**exit code 落檔量測**（`cmd > log 2>&1; echo EXIT=$?`——本 session 兩次被 `| tail` 騙）、全形標點 `${var}`（第 3 次實踩）、implementer 只跑單 case（防 assertion-baseline 互踩）

## K6 checklist（散落各波的實證項——開工必讀）

**ratchet（最難）**：
- D1-D9 差異表：`kit/engines/ratchet.sh` 檔頭
- dfaa kit.yaml **必填** `slow_counters: "eslint-warnings"`（漏填=push 從秒級變分鐘級）
- 44 counter 抽到 dfaa `.claude/kit/counters.d/`：27 個共用 helper counter 走 `NN-*.lib.sh` + 薄 wrapper；`count_crap` 內 `${script_dir}/coverage-freshness.sh` 改 `${PROJECT_ROOT}/scripts/hooks/`（不改 Step B 必炸）
- Step B 前先跑 baseline 鍵集 diff（雙向核對是 kit 新行為，上游多餘鍵靜默）
- dfaa canary 用 `--count-large-files` 原字面（糖衣已支援）

**rtm**：宣告 `rtm_nonpath_tokens: "Next.js Node.js sigma.js reference_sso_idp_claims.md Prisma.sql"` + `impl_path_bases: ". apps/web apps/web/src apps/web/prisma/schema"`（parity 實測值）；reqsync excludes 段名比對寬鬆點（`services/__tests__` 上游擋 kit 放——對帳留意）；讀側 hook 首啟觀察 `.claude/dev/.rtm-reminder-log` 誤報率

**其他**：dfaa `__tests__/canary.sh` 8 個 test_* 留 dfaa（shim 與否 K6 裁定）；hook-wiring `${ref#../../}` 剝兩層與 dfaa 耦合；retention 寫 INDEX 用 worktree 根（上游瑕疵 verbatim）；信任鏈緩解（kit.yaml 記安裝時 kit HEAD hash）K6 裁定；matrix_hash 公式三處內聯可整併 lib（選做）

**Step C shim 關鍵**：`.husky`/settings.json **路徑不動**（worktree 安全）；被吸收腳本原地改 3 行 `exec bash ~/projects/skills/kit/<path> "$@"`（.ts 用 exec tsx）。dfaa 活躍 worktree 現況要重查（`git -C ~/projects/dfaa worktree list`）。

**安全 advisory 未消化**：M2（matrix_dir 白名單驗證）、M3（marker 缺尾拒寫）→ K7；M1/M4/L×4 記錄在 K5 dashboard（已 wave-close，`git show` INDEX 取回）。

## 待處理

0. **【GPT 總檢已回：K6 有條件 No-Go——先開 K5.5 硬化波】**（全文 `.claude/dev/2026-07-31_gpt-review-k1k5.md`；GPT-5 判定）三必修：
   ①**隔離 parity harness**——狀態型腳本（lock/stamp/preauth/baseline）禁止同 worktree 串行雙跑，須隔離雙 clone 從相同 git state 對帳（exit+正規化輸出+檔案副作用+lock/stamp）；含真 Husky、session restart、shim 三行的 $@/cwd/env/exit 傳遞驗證
   ②**consumer contract**——kit.yaml 加 contract_version+kit commit pin；kit doctor 拒未知版本/恆綠 gate/空 canary/gate↔fixture 不對帳；K6 期間凍結 kit revision
   ③**provenance 語意重做**——upstream 驗歷史 blob（git show <upstream_commit>:<path>，現在 _uc 欄讀了沒用）；live 狀態另設 active/retired/successor（K6 shim 後現行比對必永久 drift）；驗 mode/hash/日期/重複 key；統一 anchor 規則（現 manifest test 與 audit anchor 契約衝突——真 anchor 列會被測試打紅）
   附修：canary 引擎對帳 gates.d（無 fixture gate 永久漏測）、pathspec 解析零條 exit 0 假 0、「空值=降級」文件實作矛盾、merge_prep_cmd 例外明列、absorbed 日期修正（07-31→07-30，我寫錯被 GPT 逐筆對出）、health.d 文件標註 K7 未交付
1. ~~GPT 總檢~~（已完成，見上）
2. **dotfiles wave 鏡像過期**：`rsync -av --delete ~/projects/skills/kit/wave/ ~/projects/dotfiles/skills/wave/` + commit（K5 動了 wave SKILL+templates）
3. K6 本體（`/wave` 開波，執行倉 **dfaa**；worktree 慣例照舊但注意 kit 的 H2 守衛——從 kit 主 checkout 消費）
4. dfaa 端既存事項：`.claude/dev/` 有 fix/patrol-round1-direct worktree 活躍中（開波時 Phase 0 會掃到）

## 關鍵檔案（新 session 先讀）

- `~/.claude/plans/witty-hugging-sphinx.md` — 總計畫（K6-K8 定義）
- `~/.claude/plans/witty-hugging-sphinx-agent-ace953f2ac0b47679.md` §6 — K6 四步遷移設計全文
- `~/projects/skills/kit/references/declaration-formats.md` — 宣告契約 SSOT（dfaa kit.yaml 照此寫）
- `~/projects/skills/kit/engines/ratchet.sh` 檔頭 D1-D9 — K6 對帳基準
- `~/projects/skills/kit/manifest/provenance.tsv` — 40 行帳（哪些 dfaa 腳本已被吸收=要 shim 的清單）
- `.claude/dev/2026-07-31_gpt-review-k1k5.md` — GPT 總檢全文（K5.5 三必修依據）

## 建議 Skills

- `/wave` — K6 開波（帶參數：K6 dfaa 遷移+checklist 摘要）
- `/askfable` — 諮詢點 1 照慣例（每波都抓到缺口，勿省）
- `/kit-audit` — K6 收尾後首跑正式版

## 快速指令

```bash
# 狀態核實
cd ~/projects/skills && git log --oneline -3 && bash kit/tests/run.sh > /tmp/suite.log 2>&1; echo EXIT=$? && tail -1 /tmp/suite.log
# GPT 總檢結果
cat /tmp/askgpt-k1k5.log | tail -80
# dfaa 現況
git -C ~/projects/dfaa worktree list && git -C ~/projects/dfaa log --oneline -3
```

請先讀總計畫 §K6 行與詳細設計 §6，再讀 GPT 總檢結果，然後 `/wave` 開 K6。
